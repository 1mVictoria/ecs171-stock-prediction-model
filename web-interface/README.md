# Stock Action Predictor Web Interface

A simple Flask‐powered frontend that wraps a pre‐trained Random Forest model to predict whether to “Hold” or “Take Action (Buy/Sell)” based on nine stock features. Users fill in today’s metrics and immediately see the model’s recommendation.

---

## Project Structure

